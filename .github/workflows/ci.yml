name: CI

on:
  pull_request:
  push:
    branches: [main, staging]

permissions:
  contents: read

jobs:
  addon-install:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: ci-password
      ODOO_DB: facodi_ci
      ODOO_ADMIN_PASSWD: ci-admin-password
      ODOO_WORKERS: 0
    steps:
      - uses: actions/checkout@v4
      - name: Validate Compose configuration
        run: docker compose -f infrastructure/docker-compose.yml config --quiet
      - name: Start PostgreSQL
        run: docker compose -f infrastructure/docker-compose.yml up -d db
      - name: Install FACODI Core on a clean database
        run: |
          docker compose -f infrastructure/docker-compose.yml run --rm odoo \
            --stop-after-init \
            --without-demo=all \
            -d "$ODOO_DB" \
            -i facodi_core
      - name: Show service logs on failure
        if: failure()
        run: docker compose -f infrastructure/docker-compose.yml logs --no-color
